using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlanetariumText : MonoBehaviour
{

    public string jupiter;
    public string saturno;
    public string marte;

   

    private bool jupiterIn = false;
    private bool saturnoIn = false;
    private bool marteIn = false;

    private float time;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
